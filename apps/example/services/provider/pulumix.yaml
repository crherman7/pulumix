# Provider Service
# Provides cluster information as outputs

metadata:
  name: provider
  version: 1.0.0
  description: Kubernetes cluster provider service
  team: platform
  contract:
    version: 1.0.0

stacks:
  local:
    clusterName: hello-world
    registry: localhost:5001
    kubeconfig: k3d-hello-world

  production:
    clusterName: production-cluster
    registry: null
