# Hello World Example
# Root configuration - services handle their own provider/ingress setup

name: hello-world

stacks:
  local:
    namespace: hello-world

  production:
    namespace: hello-world-prod
