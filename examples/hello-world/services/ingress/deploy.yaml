# Ingress Service
# Installs ingress controller (traefik, nginx, etc.)

name: ingress
dependencies:
  - provider

stacks:
  local:
    # k3d includes traefik - skip installation
    install: false

  production:
    install: true
    type: traefik
    replicas: 2
