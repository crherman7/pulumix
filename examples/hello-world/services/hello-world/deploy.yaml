# Hello World Service
name: hello-world

dependencies:
  - ingress

stacks:
  local:
    port: 3000
    replicas: 2
    env:
      NODE_ENV: development

  staging:
    port: 3000
    replicas: 3
    env:
      NODE_ENV: staging

  production:
    port: 3000
    replicas: 5
    env:
      NODE_ENV: production
